'use client'
import React from 'react'
import { useAppDispatch } from '../store/store'
import { setUser, setUserLoading } from './store'

const AuthWatcher = () => {
    const dispatch = useAppDispatch()

    return (
        <></>
    )
}

export default AuthWatcher